import BannerInstitucional, {
  Props,
} from "$store/components/ui/BannerInstitucional.tsx";

export default function HeroBannerSection(props: Props) {
  return <BannerInstitucional {...props} />;
}
